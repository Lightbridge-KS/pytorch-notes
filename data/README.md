# Data

Use this folder for reasonably sized datasets that support runnable PyTorch examples.

- `raw/`: source data as downloaded or copied
- `processed/`: cleaned or transformed data
- `external/`: third-party data snapshots or extracted examples

Large or sensitive data should stay out of git unless explicitly intended for the book.
