[project]
name = "pytorch-notes"
version = "0.1.0"
description = "Runnable PyTorch notes for a Quarto Book."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel",
    "matplotlib",
    "nbclient>=0.10.4",
    "nbformat",
    "numpy",
    "pandas",
    "pillow",
    "pyyaml>=6.0.3",
    "rich",
    "torch",
    "torchvision",
    "tqdm",
]

[dependency-groups]
dev = [
    "ruff",
]

[tool.ruff]
line-length = 100
target-version = "py312"
